# scripts/README.md

## Summary Table Generator

`make_summary_tables.py` reads experiment outputs from `evaluation_results/summary.csv` and generates `docs/summary_tables.md`.

Usage:

```bash
python make_summary_tables.py
```
